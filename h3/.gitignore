# 成片不入 git：11 条约 21 MB，二进制大文件会让仓库体积不可逆膨胀。
# 真源在复现仓库 /Users/kk/Work/chenkai_airepo/Juscent/output/（含 sha256 记录）。
# 恢复方法见 README.md §6。
#
# 🔴 注意规则**只限定在 h3/videos/**，不要写成全局 *.mp4 ——
#    全局规则会连带挡住将来可能加入的输入素材（参考视频等），
#    而且被挡住时没有任何提示，只有 git add 静默失败。
videos/*.mp4

# posters/ 体积小（共约 316 KB）且是页面首屏内容，**入 git**，不要在这里排除。
