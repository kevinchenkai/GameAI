extends Node
## 事件原始效果结算单例（autoload: EventManager）。
##
## Task 1 占位骨架；Task 5 实现：按 events.json 的 effect_type 计算原始效果。
## 只算原始效果，技能/护盾由 SkillManager 以 hook 介入（CLAUDE.md §3.6 / 主方案 §10.7）。

func _ready() -> void:
	pass
