extends Node
## 棋盘数据与坐标单例（autoload: BoardManager）。
##
## Task 1 占位骨架；Task 2 实现：加载 board_72.json、生成 72 格、提供坐标与事件查询。
## 加载时必须校验每个非空 event_id 存在于 events.json，否则 push_error 中止（CLAUDE.md §3.5）。

func _ready() -> void:
	pass
