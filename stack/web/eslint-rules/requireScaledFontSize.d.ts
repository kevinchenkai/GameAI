import type { Rule } from 'eslint';

export const requireScaledFontSizeRule: Rule.RuleModule;

export const stackpopUiPlugin: {
  readonly rules: {
    readonly 'require-scaled-font-size': Rule.RuleModule;
  };
};
